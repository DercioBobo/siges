"""
The invoice filter/query API used to live here. It's now shared with the
Payment Desk page and lives at escola.escola.invoice_query
(get_filter_options / get_invoices) — invoice_monitor.js calls that module
path directly.
"""
